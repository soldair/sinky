{
  "name": "sync-channel",
  "version": "1.0.0",
  "description": "run async node apis like http as sync",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/*.js && standard --fix"
  },
  "keywords": [
    "mkfifo",
    "sync",
    "async-to-sync"
  ],
  "author": "Ryan Day <ryaneday@google.com>",
  "license": "Apache-2.0",
  "dependencies": {
    "split2": "^3.0.0"
  },
  "devDependencies": {
    "mocha": "^5.2.0",
    "standard": "^12.0.1",
    "teeny-request": "^3.10.0"
  }
}
